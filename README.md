# Personal dotfiles
Using [chezmoi](https://www.chezmoi.io/) to manage all config files and keep them in sync in remote.

### Setting up a new machine
```bash
chezmoi init --apply sachingodishela
```
